name: TRON SR DoS PoC
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  recon:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Smoke test
        run: echo "workflow loaded"
